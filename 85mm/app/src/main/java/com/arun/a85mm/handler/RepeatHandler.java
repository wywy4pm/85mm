package com.arun.a85mm.handler;

import android.content.Context;
import android.os.Handler;
import android.os.Message;

import com.arun.a85mm.activity.SplashActivity;

/**
 * Created by Administrator on 2017/12/20.
 */

/*public class RepeatHandler extends Handler {
    private Context context;

    public RepeatHandler(Context context) {
        this.context = context;
    }

    @Override
    public void handleMessage(Message msg) {
        super.handleMessage(msg);
        if (context != null) {
            if (context instanceof SplashActivity) {
                ((SplashActivity) context).showConfigError(context);
                ((SplashActivity) context).repeatError();
            }
        }
    }
}*/
